name: Deployment
on:
  push:
    branches:
      - "*"
  workflow_call:

jobs:
  build:
    uses: ./.github/workflows/build.yml
  deploy:
    needs: build
    uses: ./.github/workflows/deploy-gh-pages.yml
